# Harsh Sood — Portfolio

Personal portfolio and project site built with Vite + React + TypeScript.

To run locally:

```bash
npm install
npm run dev
```

Deployment: this project is deployed on Vercel. Update code and push to the `main` branch to trigger a deployment.

If you need more project documentation, add it here.
Changes made via Lovable will be committed automatically to this repo.
